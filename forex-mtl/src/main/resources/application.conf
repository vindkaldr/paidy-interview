app {
  http {
    host = "0.0.0.0"
    port = 8080
    timeout = 40 seconds
  }
  forex {
    exchange-rate-expires-after = ${?EXCHANGE_RATE_EXPIRES_AFTER}
  }
  one-frame {
    host = ${?ONE_FRAME_HOST}
    port = ${?ONE_FRAME_PORT}
  }
  redis {
    host = ${?REDIS_HOST}
    port = ${?REDIS_PORT}
  }
}

